import MinifierPage from "@/components/minifier";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "JSON Minifier - JSON Tools",
  description: "Minify and compress JSON data by removing whitespace",
};

export default MinifierPage;
