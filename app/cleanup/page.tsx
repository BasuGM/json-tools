import CleanupPage from "@/components/cleanup";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "JSON Cleanup Tool - JSON Tools",
  description: "Clean and optimize your JSON data by removing nulls, empty values, and duplicates",
};

export default CleanupPage;
