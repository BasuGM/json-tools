import type { Metadata } from "next";
import AnalyzerPage from "@/components/analyzer";

export const metadata: Metadata = {
  title: "JSON Size Analyzer - JSON Tools",
  description:
    "Analyze and optimize JSON file size. Identify large properties and see size breakdowns instantly.",
};

export default AnalyzerPage;
